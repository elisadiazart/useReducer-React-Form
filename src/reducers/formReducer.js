export const INITIAL_STATE_FORM = {
        name: '',
		email:'',
		online: false
}

export const formReducer = (values, action) => {
    switch (action) {
        case name:
            return{
            }
    }
}